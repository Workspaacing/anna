name: Report a crash
description: Anna is crashing or freezing or hanging.
type: Crash
labels: "state:needs triage"
body:
  - type: textarea
    attributes:
      label: Reproduction steps
      description: A step-by-step description of how to reproduce the crash from a **clean Anna install**. The more context you provide, the easier it is to find and fix the problem fast.
      placeholder: |
        1. Start Anna
        2. Perform an action
        3. Anna crashes
    validations:
      required: true
  - type: textarea
    attributes:
      label: Anna version and system specs
      description: |
        Open the command palette in Anna, then type “anna: copy system specs into clipboard”.
      placeholder: |
        Anna: v1.0.0 (stable 71758a3f0c2d4e6b8a9c1d3e5f7a9b0c2d4e6f8a)
        OS: macOS 15.1
        Memory: 36 GiB
        Architecture: aarch64
    validations:
      required: true
  - type: textarea
    attributes:
      label: Attach Anna log file
      description: |
        Open the command palette in Anna, then type `anna: open log` to see the last 1000 lines. Or type `anna: reveal log in file manager` in the command palette to reveal the log file itself.

        ⚠️ **Please review your log file for secrets (API keys, tokens, etc.) and partially or fully redact them before posting.**
      value: |
        <details><summary>Anna.log</summary>

        <!-- Paste your log inside the code block. -->
        ```log

        ```

        </details>
    validations:
      required: false
